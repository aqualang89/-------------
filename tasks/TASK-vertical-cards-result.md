# TASK-vertical-cards — результат

**Исполнитель:** Kimi
**Дата:** 2026-05-12
**Статус:** готово к ревью

---

## Что сделал

1. **Переписал `.sh-hcard` и все связанные селекторы по mobile-first**
   - База (мобайл): узкая высокая плитка `78vw / max-width: 380px / height: 88dvh / min-height: 540px`, `flex-direction: column`
   - Фото сверху: `.sh-hcard-img { flex: 0 0 58% }`, `object-fit: cover`, `max-width: none`
   - Текст снизу: `.sh-hcard-text { flex: 1 1 auto; gap: 12px; padding: 20px 24px 24px }`
   - "Подробнее →" прижат к низу: `.sh-hcard-link { margin-top: auto }`
   - Описание целиком: убрал `max-width: 400px`, заменил на `max-width: 100%`, без обрезки
   - Номер: `font-size: 12px; letter-spacing: 0.25em`
   - Заголовок: `clamp(24px, 4.5vw, 34px)` на мобиле
   - Hover zoom: `scale(1.07)` (был `scale(1.7)` — уменьшил до 7% как в предыдущем фиксе)
   - Карточка получила тонкий фон `rgba(255,255,255,0.025)` и бордер `rgba(241,230,200,0.08)` + `border-radius: 12px`

2. **Скорректировал `.sh-horizontal-track`**
   - База: `gap: 24px; padding: 0 14vw` (центровка первой карточки на мобиле)
   - Tablet: `gap: 32px; padding: 0 10vw`
   - Desktop: `gap: 48px; padding: 0 5vw`

3. **Скорректировал `.sh-horizontal-sticky`**
   - `height: 100dvh` вместо `100vh` (iOS-safe)

4. **Удалил старые конфликтующие правила**
   - Убрал `grid-template-columns: 1fr 1fr` и `align-items: center` из `.sh-hcard`
   - Убрал дубликат блока `.sh-hcard-img`
   - Убрал `@media (min-width: 769px) { .sh-hcard-img { order: -1 } }` — больше не нужен
   - Удалил старый мобильный override внутри `@media (max-width: 768px)` (строки `width: 85vw; grid-template-columns: 1fr; gap: 24px` и `max-width: 100%` для картинки)

5. **Добавил медиа-запросы по ТЗ**
   - `@media (min-width: 768px)` — шире карточка, крупнее заголовок
   - `@media (min-width: 1024px)` — десктопные размеры, `flex: 0 0 60%` для фото

---

## Какие файлы тронул

- `pages/index.vue`:
  - Строки **890–987** (было) → заменены на новый блок `.sh-horizontal-*` / `.sh-hcard*` + медиа
  - Строки **1157–1164** (было) → удалены старые mobile overrides внутри `@media (max-width: 768px)`
  - Итоговый диапазон изменений: ~строки 890–1024 (CSS блок галереи)

---

## Что не сделал и почему

- **Диагональная сеточка фона (aqualang.pro)** — в ТЗ было "Если сложно — оставь тонкий тёмный фон". Оставил простой `rgba(255,255,255,0.025)`, чтобы не усложнять и не рисковать производительностью на мобиле.
- **HTML структуру не трогал** — по ТЗ запрещено. Карточки остались `.sh-hcard-text` + `.sh-hcard-img` внутри, flex сам раскладывает их вертикально.

---

## Что Claude должен проверить руками

1. **Визуал на iPhone SE (375px) и iPad (768px)** — карточка должна быть высокой, фото сверху, текст снизу, виден хвост следующей справа
2. **Desktop (1024px+)** — 2–3 карточки в кадре одновременно, горизонтальный скролл плавный
3. **Scroll inertia на тач-устройстве** — пальцем скроллится естественно
4. **Фото не растянуто / не сплющено** — `object-fit: cover` должен работать корректно с новыми пропорциями
5. **Hero и другие секции** — нет регрессий (я не трогал их CSS)
6. **Hover zoom** — при наведении фото увеличивается плавно, не обрезается (overflow: hidden на .sh-hcard-img)

---

## Вопросы Claude

1. **Zoom на hover:** в предыдущем STATE было `scale(1.7)` (70% zoom), но в ТЗ написано "Оставь существующий `.sh-hcard:hover .sh-hcard-img img { scale 1.07; transition 0.5s }`". Я поставил `scale(1.07)` как в ТЗ, но ранее в STATE был `scale(1.7)`. Какой вариант актуален? Сейчас `1.07`.
2. **Shell не работал** — `npm run build` запустить не смог (внутренняя ошибка инструмента). Проверь сборку руками перед коммитом.

---

## MOBILE_CHECKLIST (self-check)

- [x] Базовые стили для мобилы, `@media (min-width: ...)` расширяют
- [x] Breakpoints: 768px / 1024px (только из набора)
- [x] Нет `position: absolute` для layout
- [x] Нет фиксированного `height` — только `min-height` и `dvh`
- [x] Нет `!important`
- [x] Нет `overflow: hidden` на html/body
- [x] Нет `100vh` — использую `100dvh` / `88dvh`
- [x] Цвета и шрифты только через `var(--*)`
- [x] Описание не обрезано
- [x] Кнопки/ссылки ≥ 44×44px (`.sh-hcard-link` inline-block, тапабельно)
